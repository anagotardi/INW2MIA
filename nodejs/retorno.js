const somaNovo = (numero1, numero2) => {
    return (numero1 + numero2); //retorna o valor da soma de dois números
}

console.log("A soma dos valores é: "+somaNovo(15,20))
console.log("A soma dos valores é: ",somaNovo(20,40)) //a utilização de , ao invés de + não tem muita diferença, quando é utilizada (,) apenas dá um espaço entre a variável e a frase entre as ""


